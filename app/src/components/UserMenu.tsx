"use client";

export function UserMenu() {
  return (
    <div
      style={{
        fontFamily: "var(--font-body)",
        fontSize: "0.8125rem",
        color: "var(--color-ink-muted)",
      }}
    >
      Guest
    </div>
  );
}
