---
phase: 28-navigation-refinement
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - app/src/app/events/page.tsx
  - app/src/app/services/page.tsx
  - app/src/app/operations/page.tsx
autonomous: true
---

<objective>
Create three context-based navigation pages using the ContextCatalog component.

Purpose: Provide context-based entry points for staff to browse content by work area (Events, Services, Operations) rather than by content type.

Output: Three new route pages that group related content by work context.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-navigation-refinement/28-CONTEXT.md

# Component from Plan 01:
@app/src/components/ContextCatalog.tsx

# Context mapping (from Phase 28 planning):
# - Events: categories=["clients"] - client-facing content (timelines, layouts, planning)
# - Services: categories=["services"] - packages, menus, pricing
# - Operations: categories=["staff", "operations"] - procedures, training, facilities
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Events page</name>
  <files>app/src/app/events/page.tsx</files>
  <action>
Create the Events context page at /events route.

```typescript
"use client";

import { ContextCatalog } from "@/components/ContextCatalog";

export default function EventsPage() {
  return (
    <ContextCatalog
      title="Events"
      description="Client event planning resources - timelines, layouts, checklists, and day-of procedures"
      categories={["clients"]}
    />
  );
}
```

This page surfaces all client-facing content:
- Planning checklists
- Day-of timelines and processionals
- Venue layouts
- Booking forms documentation
  </action>
  <verify>Page renders: `cd app && bun run build`</verify>
  <done>Events page exists at /events and shows clients category content with type filters</done>
</task>

<task type="auto">
  <name>Task 2: Create Services page</name>
  <files>app/src/app/services/page.tsx</files>
  <action>
Create the Services context page at /services route.

```typescript
"use client";

import { ContextCatalog } from "@/components/ContextCatalog";

export default function ServicesPage() {
  return (
    <ContextCatalog
      title="Services"
      description="Packages, menus, and pricing information for all venue offerings"
      categories={["services"]}
    />
  );
}
```

This page surfaces all service/pricing content:
- Wedding packages
- Event packages
- Catering menus
- Add-ons and upgrades
  </action>
  <verify>Page renders: `cd app && bun run build`</verify>
  <done>Services page exists at /services and shows services category content</done>
</task>

<task type="auto">
  <name>Task 3: Create Operations page</name>
  <files>app/src/app/operations/page.tsx</files>
  <action>
Create the Operations context page at /operations route.

```typescript
"use client";

import { ContextCatalog } from "@/components/ContextCatalog";

export default function OperationsPage() {
  return (
    <ContextCatalog
      title="Operations"
      description="Staff procedures, training materials, and facility management"
      categories={["staff", "operations"]}
    />
  );
}
```

This page surfaces all internal operations content:
- Staff procedures (closing, service protocols)
- Training materials (manual, program, scripts)
- Facility management (maintenance, bar operations)
- HR forms documentation
  </action>
  <verify>Page renders: `cd app && bun run build`</verify>
  <done>Operations page exists at /operations and shows staff + operations categories</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && bun run build` succeeds
- [ ] /events route renders with clients content
- [ ] /services route renders with services content
- [ ] /operations route renders with staff + operations content
- [ ] Type filter chips work on all three pages
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Three new routes functional
- Each page correctly maps to its category/categories
- Type filters work within each context
</success_criteria>

<output>
After completion, create `.planning/phases/28-navigation-refinement/28-02-SUMMARY.md`
</output>
