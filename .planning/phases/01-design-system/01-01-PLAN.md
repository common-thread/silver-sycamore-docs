---
phase: 01-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/src/app/globals.css, app/src/app/layout.tsx]
autonomous: true
---

<objective>
Establish Typeform-inspired design tokens — bold serif typography, black/white archive aesthetic with clean readable spacing.

Purpose: Create the foundational CSS variables and typography that all subsequent UI components will use.
Output: Updated globals.css with new design tokens, layout.tsx with updated font imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@app/src/app/globals.css
@app/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens for Typeform-inspired aesthetic</name>
  <files>app/src/app/globals.css</files>
  <action>
Replace Heritage Elegance palette with Typeform-inspired archive aesthetic:

**Colors (black/white dominant):**
- `--color-ink: #191919` (near-black, primary text)
- `--color-ink-light: #555555` (secondary text)
- `--color-ink-muted: #888888` (tertiary/disabled)
- `--color-background: #FFFFFF` (primary background)
- `--color-surface: #FAFAFA` (cards, elevated surfaces)
- `--color-border: #E5E5E5` (subtle borders)
- `--color-border-strong: #CCCCCC` (emphasized borders)
- `--color-accent: #191919` (primary actions - black)
- `--color-accent-hover: #333333` (hover state)

**Typography:**
- `--font-display: 'Playfair Display', Georgia, serif` (bold serif for headings)
- `--font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif` (clean sans for body)

**Spacing (keep existing scale):**
- Keep --space-xs through --space-2xl

Update body styles:
- background: var(--color-background)
- font-family: var(--font-body)
- font-size: 16px (slightly larger for readability)

Update heading styles:
- Use --font-display
- font-weight: 700 (bold)
- Tighter letter-spacing (-0.02em)

Update link styles:
- Remove underline by default
- Add underline on hover
- Use --color-ink for text, not --color-accent

Update table styles to match new palette.

Update .prose styles for new typography.

Keep all structural CSS (focus states, selection, etc.) but update colors.
  </action>
  <verify>Run `cd app && bun run build` - no CSS errors</verify>
  <done>globals.css uses new Typeform-inspired design tokens with black/white palette and serif/sans font pairing</done>
</task>

<task type="auto">
  <name>Task 2: Update font imports in layout</name>
  <files>app/src/app/layout.tsx</files>
  <action>
Update Google Fonts link to load new fonts:
- Remove Cormorant Garamond and Source Sans 3
- Add Playfair Display (weights: 400, 500, 600, 700)
- Add Inter (weights: 400, 500, 600)

Update the font link href to:
```
https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@400;500;600;700&display=swap
```

Keep all other layout structure unchanged for now (header, nav, footer will be updated in Plan 03).
  </action>
  <verify>Run `cd app && bun run dev`, visit localhost:3000, confirm fonts load (check Network tab for Google Fonts)</verify>
  <done>Layout loads Playfair Display and Inter from Google Fonts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && bun run build` succeeds without errors
- [ ] Dev server shows new typography (bold serif headings, clean sans body)
- [ ] Black/white color scheme visible throughout
- [ ] No broken styles or missing font fallbacks
</verification>

<success_criteria>
- All tasks completed
- New design tokens established in globals.css
- Fonts properly loaded
- Visual appearance matches Typeform-inspired aesthetic (bold serif + clean sans, black/white dominant)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-01-SUMMARY.md`
</output>
