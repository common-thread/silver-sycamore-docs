---
phase: 01-design-system
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [app/src/app/layout.tsx, app/src/components/Header.tsx, app/src/components/Sidebar.tsx, app/src/components/PageHeader.tsx]
autonomous: false
---

<objective>
Create layout components and update page templates with Typeform-inspired structure.

Purpose: Establish consistent page structure (header, navigation, content areas) using new design system.
Output: Updated layout with new Header, optional Sidebar, and PageHeader components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@app/src/app/layout.tsx
@app/src/components/ui/Button.tsx
@app/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component</name>
  <files>app/src/components/Header.tsx</files>
  <action>
Create a clean Header component to replace inline header in layout.tsx:

**Structure:**
- Full-width, white background
- Left: Logo/brand area (text "Silver Sycamore" in Playfair Display, bold)
- Right: Navigation links (horizontal)
- Bottom border using --color-border

**Navigation items:**
- Home, Services, Clients, Staff, Operations, Deliverables
- Use --font-body, font-weight 500
- Uppercase or small-caps optional (match Typeform style)
- Active state: underline or bold

**Styling:**
- Padding: px-8 py-4
- No background color other than white
- Clean, minimal appearance

Add "use client" directive (for future interactivity like mobile menu).

Export as default.
  </action>
  <verify>Import Header in layout.tsx, replace inline header, confirm navigation works</verify>
  <done>Header.tsx exists with logo and navigation, exported and working</done>
</task>

<task type="auto">
  <name>Task 2: Create PageHeader component</name>
  <files>app/src/components/PageHeader.tsx</files>
  <action>
Create PageHeader for consistent page titles:

**Props:**
- title: string
- description?: string
- breadcrumbs?: { label: string; href?: string }[]
- actions?: React.ReactNode (for buttons/actions in header area)

**Structure:**
- Title: h1 using Playfair Display, font-weight 700
- Description: below title, using --color-ink-light
- Breadcrumbs: above title, small text, linked
- Actions: right-aligned (flex justify-between)

**Styling:**
- Margin-bottom for spacing from content
- Title size: text-3xl or text-4xl
- Keep clean and minimal

Can be server component (no "use client" needed unless actions require it).
  </action>
  <verify>Use PageHeader on homepage, confirm title and breadcrumbs render</verify>
  <done>PageHeader.tsx exists with title, description, breadcrumbs, and actions support</done>
</task>

<task type="auto">
  <name>Task 3: Update root layout with new components</name>
  <files>app/src/app/layout.tsx</files>
  <action>
Refactor layout.tsx to use new Header component:

**Changes:**
1. Import Header from @/components/Header
2. Replace inline header JSX with <Header />
3. Remove inline nav (now part of Header)
4. Simplify footer (keep minimal, use new design tokens)
5. Remove LogoPlaceholder import (Header handles branding)

**Structure after refactor:**
```tsx
<html>
  <body>
    <ConvexClientProvider>
      <div className="min-h-screen flex flex-col">
        <Header />
        <main className="flex-1 px-8 py-6">{children}</main>
        <footer>...</footer>
      </div>
    </ConvexClientProvider>
  </body>
</html>
```

**Footer styling:**
- Minimal: copyright text only
- Use --font-body, --color-ink-muted
- Thin top border
  </action>
  <verify>Run dev server, navigate through pages, confirm header/footer appear correctly</verify>
  <done>layout.tsx uses Header component, footer is simplified, overall structure is clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete design system refresh with new typography, colors, UI components, and layout</what-built>
  <how-to-verify>
    1. Run: cd app && bun run dev
    2. Visit: http://localhost:3000
    3. Check homepage:
       - Header has "Silver Sycamore" in bold serif font
       - Navigation links are clean and readable
       - Category cards use new Card styling
    4. Click through to any document:
       - Typography is crisp (Playfair Display headings, Inter body)
       - Black text on white background
       - Clean, minimal borders
    5. Overall feel:
       - Typeform-inspired aesthetic (bold, clean, archival)
       - No visual bugs or broken layouts
       - Consistent styling throughout
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && bun run build` succeeds
- [ ] Header.tsx exists and is used in layout
- [ ] PageHeader.tsx exists with breadcrumb support
- [ ] Layout uses new components, no inline styling for header/nav
- [ ] Visual checkpoint passed
</verification>

<success_criteria>
- All tasks completed
- Layout is clean and uses new design system
- Human verified visual appearance matches Typeform-inspired aesthetic
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-03-SUMMARY.md`
</output>
