---
phase: 01-design-system
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [app/src/app/layout.tsx, app/src/components/Header.tsx, app/src/components/PageHeader.tsx]
autonomous: false
domain: frontend-design
invoke_skills: ["frontend-design"]
---

<objective>
Create layout components and update page templates with distinctive structure.

Purpose: Establish consistent page structure (header, navigation, content areas) using new design system.
Output: Updated layout with new Header and PageHeader components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-design-system/01-01-SUMMARY.md
@.planning/phases/01-design-system/01-02-SUMMARY.md
@app/src/app/layout.tsx
@app/src/app/globals.css
</context>

<skill_usage>
**REQUIRED**: Invoke `/frontend-design` skill for layout composition decisions.

Provide:
- Layout requirements (header, navigation, content areas)
- Design tokens and components available
- Request: spatial composition, navigation styling, visual hierarchy

The skill ensures layout feels distinctive and cohesive with archive/editorial aesthetic.
</skill_usage>

<design_continuity>
**Build on Plans 01-02:**
- Use design tokens from globals.css
- Use UI components from ui/ directory where appropriate
- Maintain archive/editorial aesthetic throughout

**Layout philosophy:**
- Clean, generous whitespace
- Clear visual hierarchy
- Navigation should feel like a table of contents
- Professional, trustworthy appearance
</design_continuity>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component</name>
  <files>app/src/components/Header.tsx</files>
  <action>
1. Invoke `/frontend-design` skill for Header layout guidance
2. Create Header component:

**Structure:**
- Full-width, clean background
- Left: Brand/logo area ("Silver Sycamore" in display font)
- Right or below: Navigation links
- Subtle bottom border

**Navigation items:**
- Home, Services, Clients, Staff, Operations, Deliverables
- Should feel like document section tabs or TOC
- Clear active/current state

**Styling direction:**
- Editorial feel — think magazine header or book navigation
- Not a typical SaaS navbar
- Generous spacing, clear typography

Add "use client" directive for future interactivity.
Export as default.
  </action>
  <verify>Import Header in layout.tsx, replace inline header, test navigation</verify>
  <done>Header.tsx exists with distinctive editorial navigation</done>
</task>

<task type="auto">
  <name>Task 2: Create PageHeader component</name>
  <files>app/src/components/PageHeader.tsx</files>
  <action>
1. Invoke `/frontend-design` skill for page title styling
2. Create PageHeader:

**Props:**
- title: string
- description?: string
- breadcrumbs?: { label: string; href?: string }[]
- actions?: React.ReactNode

**Structure:**
- Title: prominent, using display font
- Description: below title, subdued
- Breadcrumbs: above title, small, linked
- Actions: right-aligned area for buttons

**Styling:**
- Clear visual hierarchy
- Appropriate spacing from content below
- Breadcrumbs should feel like document path

Can be server component unless actions require client behavior.
  </action>
  <verify>Use PageHeader on homepage, test with breadcrumbs and actions</verify>
  <done>PageHeader.tsx exists with full feature set</done>
</task>

<task type="auto">
  <name>Task 3: Refactor root layout with new components</name>
  <files>app/src/app/layout.tsx</files>
  <action>
Refactor layout.tsx:

1. Import Header from @/components/Header
2. Replace inline header/nav with <Header />
3. Remove LogoPlaceholder import
4. Simplify footer:
   - Minimal: copyright/brand text only
   - Use design tokens
   - Thin top border
5. Keep ConvexClientProvider wrapper
6. Maintain responsive structure

**After refactor:**
```tsx
<Header />
<main className="flex-1 ...">{children}</main>
<footer>...</footer>
```

Update main content padding if needed for new layout proportions.
  </action>
  <verify>Run dev server, navigate pages, confirm layout works</verify>
  <done>layout.tsx uses new Header, simplified footer, clean structure</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete design system: typography, colors, UI components, and layout</what-built>
  <how-to-verify>
    1. Run: cd app && bun run dev
    2. Visit: http://localhost:3000
    3. Check homepage:
       - Header has distinctive branding and navigation
       - Category cards use new Card styling
       - Overall composition feels editorial/archival
    4. Navigate to a document:
       - Breadcrumbs work if present
       - Typography is crisp and readable
       - Content feels like a well-designed reference
    5. Check all navigation links work
    6. Overall assessment:
       - Does this feel like a generic web app? (NO)
       - Does this feel like opening a reference book? (YES)
       - Is it professional and trustworthy? (YES)
       - Are there any visual bugs or inconsistencies? (NO)
  </how-to-verify>
  <resume-signal>Type "approved" if design system is complete, or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && bun run build` succeeds
- [ ] Header.tsx and PageHeader.tsx exist
- [ ] Layout uses new components, no inline header/nav
- [ ] Visual checkpoint passed
- [ ] All navigation works
</verification>

<success_criteria>
- All tasks completed
- Layout styled via frontend-design skill guidance
- Complete design system established (tokens → components → layout)
- Human verified entire design system
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-03-SUMMARY.md`

Record:
- Final layout structure
- Design decisions for header/navigation
- Phase 1 completion status
- Design tokens, components, and patterns established for future phases
</output>
