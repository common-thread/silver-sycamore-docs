---
phase: 01-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [app/src/components/ui/Button.tsx, app/src/components/ui/Input.tsx, app/src/components/ui/Card.tsx, app/src/components/ui/Badge.tsx]
autonomous: false
domain: frontend-design
invoke_skills: ["frontend-design"]
---

<objective>
Create core UI component library with distinctive styling — Button, Input, Card, Badge components.

Purpose: Establish reusable, consistently-styled building blocks for all UI work.
Output: New ui/ directory with styled components using design tokens from Plan 01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-design-system/01-01-SUMMARY.md
@app/src/app/globals.css
</context>

<skill_usage>
**REQUIRED**: Invoke `/frontend-design` skill for component styling decisions.

For each component, provide:
- Component purpose and variants needed
- Design tokens available from Plan 01
- Request: specific styling approach, hover/focus states, visual refinements

The skill ensures components feel distinctive and cohesive with the design system.
</skill_usage>

<design_continuity>
**Maintain aesthetic from Plan 01:**
- Use design tokens established in globals.css
- Follow the archive/editorial tone
- Components should feel like they belong in a reference book UI

**Component philosophy:**
- Minimal border-radius (0-2px) — sharp, editorial
- Generous touch targets
- Clear state transitions
- Accessible focus indicators
</design_continuity>

<tasks>

<task type="auto">
  <name>Task 1: Create Button component</name>
  <files>app/src/components/ui/Button.tsx</files>
  <action>
1. Invoke `/frontend-design` skill for Button styling guidance
2. Create Button with variants:

**Props:**
- variant: 'primary' | 'secondary' | 'ghost' (default: 'primary')
- size: 'sm' | 'md' | 'lg' (default: 'md')
- disabled?: boolean
- children: React.ReactNode
- onClick?: () => void
- type?: 'button' | 'submit'
- className?: string

**Styling direction (refine with skill):**
- Primary: dominant color, inverted text
- Secondary: outlined, subtle
- Ghost: text-only, minimal

**States:**
- Hover: intentional transition
- Disabled: clear but not harsh
- Focus: accessible outline using accent color

Use CSS variables from globals.css. Add "use client" directive.
  </action>
  <verify>Import Button in layout.tsx temporarily, render all variants</verify>
  <done>Button.tsx exists with distinctive styling guided by frontend-design skill</done>
</task>

<task type="auto">
  <name>Task 2: Create Input component</name>
  <files>app/src/components/ui/Input.tsx</files>
  <action>
1. Invoke `/frontend-design` skill for Input styling guidance
2. Create Input component:

**Props:**
- type: 'text' | 'email' | 'password' | 'search' (default: 'text')
- placeholder?: string
- value?: string
- onChange?: (e: React.ChangeEvent<HTMLInputElement>) => void
- disabled?: boolean
- error?: boolean
- label?: string
- className?: string

**Styling direction (refine with skill):**
- Clean border using design tokens
- Minimal border-radius
- Generous padding for readability
- Focus state with clear indicator
- Error state with appropriate color
- Optional label above input

Add "use client" directive.
  </action>
  <verify>Import Input, test with and without label, test error state</verify>
  <done>Input.tsx exists with label support and error state</done>
</task>

<task type="auto">
  <name>Task 3: Create Card and Badge components</name>
  <files>app/src/components/ui/Card.tsx, app/src/components/ui/Badge.tsx</files>
  <action>
1. Invoke `/frontend-design` skill for Card and Badge styling

**Card:**
Props: children, className, padding ('none' | 'sm' | 'md' | 'lg')

Styling:
- Surface background
- Subtle border
- Minimal shadow (editorial, not floating)
- Sharp corners (0-2px radius)

**Badge:**
Props: variant ('default' | 'success' | 'warning' | 'error'), children, className

Styling:
- Small, inline display
- Tight padding
- Color variants that fit the palette
- Sharp corners

Both can be server components (no "use client" needed).
  </action>
  <verify>Import and render Card with content, Badge with all variants</verify>
  <done>Card.tsx and Badge.tsx exist with appropriate variants</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Core UI component library (Button, Input, Card, Badge)</what-built>
  <how-to-verify>
    1. Run: cd app && bun run dev
    2. Create a temporary test page or add to homepage:
       - Render Button variants (primary, secondary, ghost) in all sizes
       - Render Input with label and without, with error state
       - Render Card with content
       - Render Badge variants
    3. Check:
       - Components match design system aesthetic
       - States (hover, focus, disabled, error) work correctly
       - Typography and colors are consistent
       - Components feel cohesive and distinctive
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe what needs adjustment</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && bun run build` succeeds
- [ ] All four components exist in app/src/components/ui/
- [ ] Components use design tokens from globals.css
- [ ] Visual checkpoint passed
</verification>

<success_criteria>
- All tasks completed
- Components styled via frontend-design skill guidance
- Components are reusable and accept className for extension
- Human verified and approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-02-SUMMARY.md`

Record:
- Styling decisions made for each component
- Any deviations from initial plan
- Notes for future component development
</output>
