---
phase: 23-ui-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [app/src/styles/baseline.css, app/src/app/globals.css, app/src/components/*.tsx]
autonomous: false
domain: frontend-design
---

<objective>
Create a VISIBLE UI refresh with a new active style guide variant.

**CRITICAL CORRECTION FROM PLAN 23-01:** The previous plan added 600+ lines of CSS utility classes but components weren't updated to USE them. Result: no visible changes. This plan requires BOTH creating the variant AND applying it to components.

Purpose: Transform the visual experience through actual component modifications, not just CSS definitions.

Output:
1. Archived baseline style guide (preserved for reference)
2. New active style variant in globals.css
3. Components visually updated with new styling
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ui-refresh/23-CONTEXT.md

# Current style guide (to be archived, then replaced)
@app/src/app/globals.css

# Reference variant (wedding app adaptation)
# Location: ../wedding-package-contract-generator/app/globals.css

# KEY COMPONENTS - These MUST be visually modified
@app/src/components/Header.tsx
@app/src/components/ContentBox.tsx
@app/src/components/DocumentViewer.tsx
@app/src/components/QuickActionNav.tsx
@app/src/components/FormBuilder.tsx
@app/src/components/FormRenderer.tsx
@app/src/components/ui/Input.tsx
@app/src/components/ui/Select.tsx
@app/src/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive baseline style guide</name>
  <files>app/src/styles/baseline.css, app/src/app/globals.css</files>
  <action>
1. Create `app/src/styles/` directory if it doesn't exist
2. Copy current `app/src/app/globals.css` to `app/src/styles/baseline.css`
3. Add header comment to baseline.css:
   ```css
   /*
    * BASELINE STYLE GUIDE - Editorial Archive Foundation
    * Archived: 2026-01-18
    * Purpose: Original style tokens before UI refresh
    * DO NOT MODIFY - This is the reference baseline
    */
   ```
4. Remove the orphaned utility classes from globals.css that 23-01 added (lines that start with `.btn-`, `.card-`, `.badge-`, `.form-`, `.list-`, `.procedure-`, `.quick-action-`, `.completion-`, `.empty-`, `.skeleton-`, `.spinner`, `.overlay`, `.modal`, `.page-title`, `.page-subtitle`, `.content-section`, `.progress-bar`, `.step-indicator`, `.checkbox`) - these aren't being used

Result: Clean baseline preserved, globals.css ready for proper refresh
  </action>
  <verify>Both files exist: `ls app/src/styles/baseline.css app/src/app/globals.css`</verify>
  <done>Baseline archived at app/src/styles/baseline.css, globals.css cleaned of unused utility classes</done>
</task>

<task type="auto">
  <name>Task 2: Invoke frontend-design skill with EXPLICIT component requirements</name>
  <files>app/src/app/globals.css, app/src/components/*.tsx</files>
  <action>
Use the frontend-design skill with these EXPLICIT requirements:

**Skill invocation:** `/frontend-design`

**MANDATORY CONTEXT:**
1. This is a staff wiki+ (procedures, references, checklists, guides, forms)
2. Optimized for daily internal use, all-day readability, information-dense content
3. Base aesthetic: Editorial Archive (Playfair + DM Sans, champagne accents, warm paper tones)
4. Reference: `../wedding-package-contract-generator/app/globals.css` for adaptation pattern

**CRITICAL REQUIREMENT - APPLY TO COMPONENTS:**
Previous attempt FAILED because CSS was added but not applied. You MUST:
- Modify globals.css with new/updated styles
- **AND** update component TSX files to USE those styles
- Every style definition MUST have corresponding className usage in components

**Components that MUST be visually updated:**
| Component | Current State | Required Changes |
|-----------|---------------|------------------|
| Header.tsx | Inline styles | Use CSS classes for nav, logo, links |
| ContentBox.tsx | Basic wrapper | Add visual hierarchy, variants |
| QuickActionNav.tsx | Inline styles | Card styling, hover effects, icons |
| DocumentViewer.tsx | Minimal styling | Typography, spacing, readability |
| FormBuilder.tsx | Functional only | Polish field cards, drag indicators |
| FormRenderer.tsx | Minimal | Form field styling, submit button |
| Input.tsx | Basic | Focus states, validation states |
| Select.tsx | Basic | Dropdown styling, options |
| Button.tsx | Basic/missing | Primary, secondary, ghost variants |

**VISIBLE CHANGES REQUIRED:**
- Typography must be noticeably refined
- Spacing/padding must be systematically improved
- Interactive elements must have clear hover/focus states
- Cards/containers must have appropriate shadows/borders
- Color usage must be consistent with Editorial Archive palette

**NO ORPHANED CSS:**
Every CSS class you add to globals.css MUST be used by at least one component.
Do not add utility classes "for future use."
  </action>
  <verify>bun run build succeeds in app/ directory</verify>
  <done>
1. globals.css updated with staff hub variant styling
2. At minimum 5 components have visible className changes
3. Build passes
4. NO unused CSS classes added
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete UI refresh with style variant applied to components</what-built>
  <how-to-verify>
**Self-verification via browser automation (Claude performs this):**

1. Start app: `cd app && bun run dev`
2. Visit http://localhost:3000
3. Take screenshots of:
   - Home page (Quick Actions, navigation, overall feel)
   - `/procedures` page (document list styling)
   - `/forms` page (form cards, badges)
   - A document viewer page (typography, spacing)
   - Form builder (`/forms/new`) (field cards, buttons)
4. Save screenshots to `.planning/phases/23-ui-refresh/screenshots/`
5. Compare BEFORE vs AFTER:
   - Are buttons visually distinct (primary vs secondary)?
   - Do cards have proper shadows/borders?
   - Is typography refined (better hierarchy)?
   - Are hover states visible and smooth?
   - Does it look like a professional internal tool?

**Verification criteria:**
- [ ] Visible typography improvements
- [ ] Clear button variants
- [ ] Card/container visual polish
- [ ] Consistent spacing
- [ ] Editorial Archive aesthetic maintained
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe issues for iteration</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] app/src/styles/baseline.css exists with original globals.css content
- [ ] bun run build succeeds
- [ ] No TypeScript errors
- [ ] At least 5 components have className changes in their TSX files
- [ ] globals.css has no orphaned/unused classes
- [ ] Screenshots captured showing visible improvements
</verification>

<success_criteria>

- Baseline style guide archived and preserved
- New variant active in globals.css
- Components VISUALLY CHANGED (not just CSS definitions)
- At minimum 5 component files modified with new classNames
- Build passes without errors
- Screenshots demonstrate visible improvement
- User approves the visual result
</success_criteria>

<output>
After completion, create `.planning/phases/23-ui-refresh/23-02-SUMMARY.md`
</output>
