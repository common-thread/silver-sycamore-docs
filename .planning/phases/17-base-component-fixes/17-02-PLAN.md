---
phase: 17-base-component-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/Header.tsx
  - app/src/components/DocumentViewer.tsx
autonomous: true
---

<objective>
Adopt design tokens in high-priority user-facing components: Header and DocumentViewer.

Purpose: These are the most visible components establishing the app's visual identity. Fixing their 16 violations (8 each) ensures the primary UI surfaces use the design system consistently.

Output: Header.tsx and DocumentViewer.tsx using design tokens exclusively for spacing, typography, and transitions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-code-mapping-style-audit/STYLE-VIOLATIONS.md
@app/src/components/Header.tsx
@app/src/components/DocumentViewer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Header.tsx token violations</name>
  <files>app/src/components/Header.tsx</files>
  <action>
Replace hardcoded values with design tokens. From STYLE-VIOLATIONS.md:

**Spacing (4 violations):**
- Line 42: `"1.25rem 2rem"` → `"var(--space-5) var(--space-8)"`
- Line 77: `"1.5rem"` → `"var(--space-6)"`
- Line 87: `"0 2rem"` → `"0 var(--space-8)"`
- Line 108: `"0.875rem 1.5rem"` → `"var(--space-3) var(--space-6)"`

**Typography (3 violations):**
- Line 110: `"0.8125rem"` → `"var(--text-xs)"`
- Line 111: `600`/`500` → `"var(--font-semibold)"`/`"var(--font-medium)"`
- Line 112: `"0.04em"` → `"var(--tracking-wide)"`

**Transition (1 violation):**
- Line 120: `"0.15s ease"` → `"var(--duration-fast) var(--ease-default)"`

Note: Line numbers are approximate. Search for the actual values and replace them contextually.
  </action>
  <verify>grep for hardcoded values like "1.25rem", "0.8125rem", "0.15s ease" in Header.tsx returns no results</verify>
  <done>Header.tsx uses design tokens for all spacing, typography, and transitions. 8 violations fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix DocumentViewer.tsx token violations</name>
  <files>app/src/components/DocumentViewer.tsx</files>
  <action>
Replace hardcoded values with design tokens. From STYLE-VIOLATIONS.md:

**Typography (4 violations):**
- Line 34: `"1.75rem"` → `"var(--text-2xl)"`
- Line 35: `600` → `"var(--font-semibold)"`
- Line 64: `"0.875rem"` → `"var(--text-sm)"`
- Line 70: `"0.875rem"` → `"var(--text-sm)"`

**Spacing (3 violations):**
- Line 36: `"0.5rem"` → `"var(--space-2)"`
- Line 43: `"1.5rem"` → `"var(--space-6)"`
- Line 57: `"0.75rem 1rem"` → `"var(--space-3) var(--space-4)"`

**Border (1 violation):**
- Line 50: `"4px"` border-radius → `0` (brand aesthetic decision from Phase 16)

Note: Border-radius should be 0 consistently per brand aesthetic decision. Line numbers are approximate.
  </action>
  <verify>grep for hardcoded values like "1.75rem", "0.875rem", "4px" border-radius in DocumentViewer.tsx returns no results</verify>
  <done>DocumentViewer.tsx uses design tokens for all typography, spacing, and has 0 border-radius. 8 violations fixed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Header.tsx has no hardcoded rem/em values for spacing or typography
- [ ] DocumentViewer.tsx has no hardcoded rem/em values for spacing or typography
- [ ] Border-radius in DocumentViewer.tsx is 0 (not 4px)
- [ ] `bun run build` in app/ succeeds without errors
</verification>

<success_criteria>

- All 16 violations in Header and DocumentViewer fixed
- Both components use design tokens exclusively
- Border-radius follows brand aesthetic (0)
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-base-component-fixes/17-02-SUMMARY.md`
</output>
