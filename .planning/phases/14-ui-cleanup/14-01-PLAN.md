---
phase: 14-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/DocumentViewer.tsx
  - app/src/app/catalog/page.tsx
autonomous: true
---

<objective>
Remove developer-facing technical indicators from the document UI to create a cleaner staff experience.

Purpose: Staff should see documents without file type labels, technical emojis, or source file paths that expose implementation details.
Output: Clean document viewer and catalog page with user-friendly presentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files
@app/src/components/DocumentViewer.tsx
@app/src/app/catalog/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up DocumentViewer binary file placeholder</name>
  <files>app/src/components/DocumentViewer.tsx</files>
  <action>
In the binary file fallback section (lines 82-155), make these changes:

1. **Remove file type emojis** (lines 117-127):
   - Remove the entire div containing the emoji conditionals

2. **Remove generic type label** (lines 84-88, 128-137):
   - Remove the `typeLabels` object
   - Remove the h3 that displays "Word Document", "Excel Spreadsheet", etc.

3. **Remove source file path display** (lines 138-141):
   - Remove the paragraph showing `document.sourceFile`

4. **Improve placeholder message** (lines 143-151):
   - Change "Document preview coming soon" to something cleaner like:
     "This document is available for download."
   - Keep the existing Download link prominent

**Result:** The binary file card should show:
- Document title (already shown above)
- Document description (already shown above)
- A clean card with just the download link and brief message
- No emojis, no type labels, no file paths

**Keep:** The Download link functionality, the iframe viewer for files with URLs
  </action>
  <verify>Run `cd app && npx tsc --noEmit` to verify no TypeScript errors</verify>
  <done>DocumentViewer no longer displays emojis, type labels, or source file paths. Clean placeholder with download option.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Type column from catalog page</name>
  <files>app/src/app/catalog/page.tsx</files>
  <action>
Remove the "Type" column from the document catalog table:

1. **Remove column header** (line 51):
   - Delete `<th>Type</th>`

2. **Remove column data** (line 68):
   - Delete `<td className="text-[#586069]">{doc.sourceType || "md"}</td>`

**Result:** Table shows only Document and Subcategory columns, no technical file type info.
  </action>
  <verify>Run `cd app && npx tsc --noEmit` to verify no TypeScript errors</verify>
  <done>Catalog page no longer shows Type column with raw file extensions.</done>
</task>

<task type="auto">
  <name>Task 3: Visual verification build</name>
  <files>app/src/components/DocumentViewer.tsx, app/src/app/catalog/page.tsx</files>
  <action>
Run build and verify changes work correctly:

1. Run full build: `cd app && bun run build`
2. Check no warnings about removed code
3. Verify both files compile without errors

**Note:** No functional changes, just removal of UI elements. The core document viewing and catalog functionality remains unchanged.
  </action>
  <verify>Build completes successfully with `cd app && bun run build`</verify>
  <done>Application builds without errors. UI cleanup complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No TypeScript errors: `cd app && npx tsc --noEmit`
- [ ] Build succeeds: `cd app && bun run build`
- [ ] DocumentViewer.tsx no longer contains emojis
- [ ] DocumentViewer.tsx no longer shows typeLabels or sourceFile
- [ ] catalog/page.tsx no longer has Type column
</verification>

<success_criteria>

- Document viewer shows clean placeholder without technical indicators
- Catalog page shows only Document and Subcategory columns
- No emojis, file type labels, or source file paths visible to users
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-cleanup/14-01-SUMMARY.md`
</output>
