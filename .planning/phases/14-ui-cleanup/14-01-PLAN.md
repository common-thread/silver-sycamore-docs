---
phase: 14-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/Header.tsx
autonomous: true
---

<objective>
Consolidate the split two-tier navigation into a single flat feature-based nav, following the brand style guide.

Purpose: Staff should see all app features at the same level — Knowledge Base categories, Messages, Forms, and My Workspace — without the confusing split between brand bar and nav row.
Output: Clean single-row navigation reflecting the actual app structure, styled per brand foundation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ui-cleanup/14-CONTEXT.md
@.planning/phases/13-brand-foundation/13-01-SUMMARY.md

# Brand Foundation (CRITICAL - follow these exactly)
@.planning/brand-package/STYLE-GUIDE.md
@app/src/app/globals.css
@app/src/app/style-guide/page.tsx

# Source files
@app/src/components/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate navigation into single flat row</name>
  <files>app/src/components/Header.tsx</files>
  <action>
Restructure Header.tsx to merge the two navigation tiers into one flat nav:

**Current structure (two tiers):**
- Brand bar: Logo + [Search] + [My Workspace] [Forms] [Messages] [Notifications] [User]
- Nav row: Home | Services | Clients | Staff | Operations | Deliverables | Catalog

**New structure (single tier):**
- Brand bar: Logo + "Staff Hub" + [Search] + [Notifications] + [User]
- Nav row: Home | Services | Clients | Staff | Operations | Deliverables | Messages | Forms | My Workspace

**Changes:**

1. **Remove feature links from brand bar** (lines 77-166):
   - Remove the My Workspace link with folder icon
   - Remove the Forms link with document icon
   - Remove the Messages link with chat icon
   - Keep: SearchBar, NotificationBell, UserMenu

2. **Add features to navItems array** (lines 11-19):
   - Add `{ href: "/messages", label: "Messages" }`
   - Add `{ href: "/forms", label: "Forms" }`
   - Add `{ href: "/workspace", label: "My Workspace" }`
   - Remove `{ href: "/catalog", label: "Catalog" }` (redundant with category pages)

3. **Follow brand style guide tokens:**
   - Text: `--color-ink-light` for inactive, `--color-accent` for active
   - Font: `--font-body` (DM Sans), `--text-xs` size, `--tracking-wider` spacing
   - Active underline: `--color-accent` (champagne-deep #8B7355)
   - Hover: `--color-ink` text, `--color-border-strong` underline
   - Background: `--color-surface-dim` for nav row

4. **Preserve editorial aesthetic:**
   - Uppercase nav labels with letter-spacing
   - Subtle 2px underline for active state
   - Smooth hover transitions (0.15s ease)

**Keep:** Logo, "Staff Hub" label, SearchBar, NotificationBell, UserMenu in brand bar.
**Move:** Messages, Forms, My Workspace links from brand bar icons to nav row text links.
**Remove:** Catalog link (documents are browsed via category pages, not a separate catalog).
  </action>
  <verify>Run `cd app && npx tsc --noEmit` to verify no TypeScript errors</verify>
  <done>Navigation is a single flat row with all app features, styled per brand guide.</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and visual consistency</name>
  <files>app/src/components/Header.tsx</files>
  <action>
Run build to verify changes compile correctly:

1. Run: `cd app && bun run build`
2. Verify no errors or warnings about Header.tsx
3. Confirm all nav items render at the same level in the nav row

**Expected nav items (in order):**
Home, Services, Clients, Staff, Operations, Deliverables, Messages, Forms, My Workspace

**Style verification against brand guide:**
- All items use same uppercase styling (--text-xs, --tracking-wider)
- Active state: --color-accent text with 2px underline
- Inactive state: --color-ink-light text
- Hover: --color-ink text with --color-border-strong underline
- Consistent padding (0.875rem vertical, 1.5rem horizontal)
  </action>
  <verify>Build completes successfully with `cd app && bun run build`</verify>
  <done>Header builds without errors. All nav items display at the same visual level with brand-compliant styling.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No TypeScript errors: `cd app && npx tsc --noEmit`
- [ ] Build succeeds: `cd app && bun run build`
- [ ] Brand bar contains only: Logo, Staff Hub label, Search, Notifications, User
- [ ] Nav row contains: Home, Services, Clients, Staff, Operations, Deliverables, Messages, Forms, My Workspace
- [ ] No feature links with icons remain in brand bar
- [ ] All nav items use brand guide tokens (--color-ink-light, --color-accent, --font-body)
</verification>

<success_criteria>

- Navigation consolidated into single flat row
- All app features visible at same level
- Styling follows brand style guide exactly
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-cleanup/14-01-SUMMARY.md`
</output>
